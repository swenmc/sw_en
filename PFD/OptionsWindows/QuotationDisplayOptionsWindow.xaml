<Window x:Class="PFD.QuotationDisplayOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:PFD"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Quotation Options" Height="375" Width="400">
    <Window.Resources>
        <System:Double x:Key="theTextBoxWidth">90</System:Double>
        <System:Double x:Key="theTextBoxWidthLarge">150</System:Double>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Name="GridDisplayOptions" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayMembers, UpdateSourceTrigger=PropertyChanged}">Members</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayPlates, UpdateSourceTrigger=PropertyChanged}">Plates</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayConnectors, UpdateSourceTrigger=PropertyChanged}">Connectors (Anchors, Bolts, Screws, Rivets)</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayBoltNuts, UpdateSourceTrigger=PropertyChanged}">Bolt Nuts</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayCladding, UpdateSourceTrigger=PropertyChanged}">Cladding</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayFibreglass, UpdateSourceTrigger=PropertyChanged}">Fibreglass</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayRoofNetting, UpdateSourceTrigger=PropertyChanged}">Roof Netting</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayDoorsAndWindows, UpdateSourceTrigger=PropertyChanged}">Doors and Windows</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayGutters, UpdateSourceTrigger=PropertyChanged}">Gutters</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayDownpipe, UpdateSourceTrigger=PropertyChanged}">Downpipes</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayFlashing, UpdateSourceTrigger=PropertyChanged}">Flashing</CheckBox>
                <CheckBox Margin="0 0 0 5" IsChecked="{Binding DisplayPackers, UpdateSourceTrigger=PropertyChanged}">Packers</CheckBox>
                <CheckBox Margin="0 0 0 20" IsChecked="{Binding DisplayCladdingAccesories, UpdateSourceTrigger=PropertyChanged}">Cladding Accesories</CheckBox>
                <CheckBox Margin="0 0 0 0" IsChecked="{Binding CalculateFreightAuto, UpdateSourceTrigger=PropertyChanged}">Calculate Freight Automatically</CheckBox>
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 0 5" >
                
                <Button Width="80" Height="25" Name="BtnOK" Margin="0 0 0 0" Click="BtnOK_Click">OK</Button>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>