<UserControl x:Class="PFD.UC_ComponentList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PFD"
        mc:Ignorable="d"
        Height="220" Width="750">
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="220"/>
        </Grid.RowDefinitions>
        <DataGrid Name ="Datagrid_Components" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                  IsReadOnly="false" AutoGenerateColumns="False" ItemsSource="{Binding Source=componentsList}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Prefix}" Header="Prefix" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding ComponentName}" Header="ComponentName" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Section}" Header="Section" Width="*"></DataGridTextColumn>
                <!--<DataGridComboBoxColumn SelectedItemBinding="{Binding Path=Section}"
                                ItemsSource="{Binding Path=Sections}" Header="Section" Width="*">
                </DataGridComboBoxColumn>-->
                <DataGridTextColumn Binding="{Binding Material}" Header="Material" Width="*"></DataGridTextColumn>
                <DataGridCheckBoxColumn Binding="{Binding Generate}" Header="Generate" Width="*"></DataGridCheckBoxColumn>
                <DataGridCheckBoxColumn Binding="{Binding Display}" Header="Display" Width="*"></DataGridCheckBoxColumn>
                <DataGridCheckBoxColumn Binding="{Binding Calculate}" Header="Calculate" Width="*"></DataGridCheckBoxColumn>
                <DataGridCheckBoxColumn Binding="{Binding Design}" Header="Design" Width="*"></DataGridCheckBoxColumn>
                <DataGridCheckBoxColumn Binding="{Binding MaterialList}" Header="MaterialList" Width="*"></DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        
    </Grid>
</UserControl>
